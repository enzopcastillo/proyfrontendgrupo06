import { ValidacionDirective } from './validacion.directive';

describe('ValidacionDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidacionDirective();
    expect(directive).toBeTruthy();
  });
});
